<!-- <!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
  <meta charset="UTF-8">
  <title>حساب من | مانتو داپر</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.rtl.min.css" rel="stylesheet"> -->

  <style>
    body {
      background-color: #faf7ff;
    }

    .dashboard-container {
      display: flex;
      flex-wrap: wrap;
      min-height: 80vh;
      margin-top: 30px;
    }

    /* ستون سمت راست */
    .sidebar {
      width: 250px;
      background: linear-gradient(180deg, #f5e6ff, #ede7f6);
      padding: 25px;
      border-radius: 20px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.05);
      margin-inline-start: 20px;
    }

    .sidebar h4 {
      text-align: center;
      color: #6a1b9a;
      font-weight: bold;
      margin-bottom: 25px;
    }

    .sidebar button {
      width: 100%;
      background: #e1bee7;
      border: none;
      padding: 10px;
      border-radius: 10px;
      margin-bottom: 10px;
      color: #4a148c;
      font-weight: 600;
      transition: 0.3s;
    }

    .sidebar button.active,
    .sidebar button:hover {
      background: #8e24aa;
      color: white;
    }

    /* محتوای سمت چپ */
    .content {
      flex: 1;
      background: #fff;
      border-radius: 20px;
      padding: 30px;
      margin: 20px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    }

    .table thead {
      background-color: #f3e5f5;
    }

    .btn-details {
      background-color: #ab47bc;
      color: white;
      border: none;
      border-radius: 8px;
      padding: 5px 12px;
      transition: 0.2s;
    }

    .btn-details:hover {
      background-color: #8e24aa;
    }

    .btn-update {
      background-color: #9575cd;
      color: white;
      border: none;
      border-radius: 8px;
      padding: 8px 16px;
    }

    .btn-update:hover {
      background-color: #7e57c2;
    }

    .form-label {
      font-weight: 600;
    }
  </style>
<!-- </head>
<body> -->

  <!-- 🔹 هدر -->
  <%- include('partials/header') %>

  <div class="container">
    <div class="dashboard-container dir-rtl">

      <!-- ستون سمت راست -->
      <div class="sidebar">
        <h4>👤 حساب من</h4>
        <button id="btn-orders" class="active">🛍 سفارش‌های من</button>
        <button id="btn-info">ℹ️ اطلاعات کاربر</button>
        <form action="/logout" method="POST">
          <button type="submit">🚪 خروج از حساب</button>
        </form>
      </div>

      <!-- محتوای سمت چپ -->
      <div class="content">
        <!-- 🔸 سفارش‌ها -->
        <div id="section-orders">
          <h4 class="mb-4">🛒 سفارش‌های من</h4>
          <% if (orders.length === 0) { %>
            <p class="text-muted">شما هنوز سفارشی ثبت نکرده‌اید.</p>
          <% } else { %>


            <!-- در سرور اضافه شود -->
            <div class="table-responsive">
            <table class="table table-bordered text-center align-middle">
              <thead>
                <tr>
                  <th>کد سفارش</th>
                  <th>تاریخ</th>
                  <th>جمع کل</th>
                  <th>وضعیت</th>
                  <th>جزئیات</th>
                </tr>
              </thead>
              <tbody>
                <% orders.forEach(order => { %>
                  <tr>
                    <td><%= order.orderCode || order._id %></td>
                    <td><%= order.createdAt.toLocaleDateString('fa-IR') %></td>
                    <td><%= order.total.toLocaleString() %> تومان</td>
                    <td><%= order.status %></td>
                    <td><a href="/orders/<%= order._id %>" class="btn btn-details btn-sm">مشاهده جزئیات</a></td>
                  </tr>
                <% }) %>
              </tbody>
            </table>
            </div>
            <!-- در سرور اضافه شود -->


            
          <% } %>
        </div>

        <!-- 🔸 اطلاعات کاربر -->
        <div id="section-info" class="d-none">
          <h4 class="mb-4">👤 اطلاعات کاربر</h4>

          <form action="/account/update" method="POST">
            <div class="row g-3">
              <div class="col-md-6">
                <label class="form-label">نام</label>
                <input type="text" name="firstName" value="<%= user.firstName %>" class="form-control">
              </div>
              <div class="col-md-6">
                <label class="form-label">نام خانوادگی</label>
                <input type="text" name="lastName" value="<%= user.lastName %>" class="form-control">
              </div>
              <div class="col-md-6">
                <label class="form-label">شماره موبایل</label>
                <input type="text" name="mobile" value="<%= user.mobile %>" class="form-control" readonly>
              </div>
              <div class="col-md-6">
                <label class="form-label">ایمیل</label>
                <input type="email" name="email" value="<%= user.email || '' %>" class="form-control">
              </div>
              <div class="col-md-6">
                <label class="form-label">آدرس</label>
                <input type="text" name="address" value="<%= user.address || '' %>" class="form-control">
              </div>
              <div class="col-md-6">
                <label class="form-label">کد پستی</label>
                <input type="text" name="postalCode" value="<%= user.postalCode || '' %>" class="form-control">
              </div>
            </div>
            <div class="mt-4 text-end">
              <button type="submit" class="btn-update">💾 بروزرسانی اطلاعات</button>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>

  <!-- 🔹 فوتر -->
  <%- include('partials/footer') %>

  <script>
    const btnOrders = document.getElementById('btn-orders');
    const btnInfo = document.getElementById('btn-info');
    const sectionOrders = document.getElementById('section-orders');
    const sectionInfo = document.getElementById('section-info');

    btnOrders.addEventListener('click', () => {
      btnOrders.classList.add('active');
      btnInfo.classList.remove('active');
      sectionOrders.classList.remove('d-none');
      sectionInfo.classList.add('d-none');
    });

    btnInfo.addEventListener('click', () => {
      btnInfo.classList.add('active');
      btnOrders.classList.remove('active');
      sectionInfo.classList.remove('d-none');
      sectionOrders.classList.add('d-none');
    });
  </script>

<!-- </body>
</html> -->
